#ifndef AUDIO_H
#define AUDIO_H

#include <vector>
#include "SDL.h"
#include "snake.h"

class AudioPlayer {
 public:
  AudioPlayer();
  ~AudioPlayer();
 
  // play sound effect
  // play music (maybe try with csound)
  
 private:

};

#endif